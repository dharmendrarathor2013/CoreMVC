@model IEnumerable<Rathor.Models.TaskType>
@{
    ViewData["Title"] = "Index";
}
@{
    Layout = "_Layout";
}

<div class="container p-3">
    <div class="row p-4">
        <div class="col p-6">
            <h2 class="text-primary">Task Type List</h2>
        </div>
        <div class="col 6 text-right">
            <a asp-controller="TaskType" asp-action="Create" class="btn btn-primary">
                <i class="fas fa-plus"></i> &nbsp;  Create New Task Type
            </a>
        </div>
    </div>

    <br /><br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>
                        Task Type 
                    </th>
                    <th width="20%" style="padding-left:50px;">
                        Operations
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Name</td>
                        <td class="text-center">
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="TaskType" asp-route-Id="@item.TypeID" asp-action="Edit" class="btn btn-primary mx-1">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <a asp-controller="TaskType" asp-route-Id="@item.TypeID" asp-action="Delete" class="btn btn-danger mx-1">
                                    <i class="fas fa-trash-alt"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No TaskType Exits.</p>
    }

</div>


